<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>منصة التفوق الدراسي 2026/2025 - تحليلات وإنجاز</title>
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;900&display=swap" rel="stylesheet">
    
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Confetti Library -->
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Cairo', 'sans-serif'],
                    },
                    colors: {
                        brand: {
                            50: '#f0fdfa',
                            100: '#ccfbf1',
                            500: '#14b8a6', // Teal
                            600: '#0d9488',
                            900: '#134e4a',
                        },
                        accent: {
                            500: '#f59e0b', // Amber for rewards
                            600: '#d97706',
                        }
                    },
                    animation: {
                        'float': 'float 5s ease-in-out infinite',
                        'fade-in-up': 'fadeInUp 0.3s ease-out forwards',
                    },
                    keyframes: {
                        float: {
                            '0%, 100%': { transform: 'translateY(0)' },
                            '50%': { transform: 'translateY(-20px)' },
                        },
                        fadeInUp: {
                            '0%': { opacity: '0', transform: 'translateY(20px)' },
                            '100%': { opacity: '1', transform: 'translateY(0)' },
                        }
                    }
                }
            }
        }
    </script>

    <style>
        /* Custom Chart Container Styles */
        .chart-wrapper-custom {
            position: relative;
            width: 100%;
            height: 300px;
            margin-bottom: 2rem;
        }

        .card-hover {
            transition: all 0.3s ease;
        }
        .card-hover:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px -4px rgba(0, 0, 0, 0.1), 0 5px 5px -3px rgba(0, 0, 0, 0.04);
        }
        
        /* Custom Scrollbar for responsiveness */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #f1f1f1; 
        }
        ::-webkit-scrollbar-thumb {
            background: #cbd5e1; 
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #94a3b8; 
        }

        /* Utility for Active Tab */
        .tab-active {
            position: relative;
            color: #ffffff !important;
            background-color: #0d9488 !important;
            box-shadow: 0 4px 10px rgba(13, 148, 136, 0.4);
        }

        /* ----- تنسيق صندوق المكافأة الجديد ( reward-box-html ) ----- */
        .reward-link-embedded {
            display: inline-block;
            padding: 0.5rem 1rem;
            border-radius: 9999px; 
            font-weight: 700;
            /* تدرج لوني ذهبي واضح */
            background-image: linear-gradient(90deg, #ffdd77, #f7d268); 
            color: #333;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.06);
            transition: all 0.2s ease;
            text-decoration: none; 
            cursor: pointer;
        }
        .reward-link-embedded:hover {
            transform: translateY(-1px);
            box-shadow: 0 8px 15px -3px rgba(255, 158, 11, 0.4);
        }
        /* --------------------------------------------------- */

        /* Modal Backdrop */
        .modal-backdrop {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(3px);
            z-index: 50;
            transition: opacity 0.3s ease;
            cursor: pointer; /* Indicates clickable backdrop */
        }
        .modal-content {
            z-index: 60;
            cursor: default; /* Reset cursor inside modal */
        }
        
        /* Calendar Grid */
        .calendar-grid {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 5px;
        }
        .calendar-day-header {
            font-size: 0.8rem;
            font-weight: 700;
            color: #4b5563;
        }
        .calendar-date-box {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 8px 4px;
            border-radius: 8px;
            transition: all 0.2s; 
            cursor: pointer;
            min-height: 70px;
            position: relative;
            overflow: hidden;
        }
        .calendar-date-box:hover {
            transform: scale(1.02);
            z-index: 10;
        }
        
        /* تمييز اليوم الحالي في التقويم */
        .current-day {
            background-color: #e0f7fa; 
            border: 2px solid #00bcd4; 
            font-weight: bold;
        }

        /* Loading Spinner */
        .spinner {
            border: 4px solid rgba(0, 0, 0, 0.1);
            border-left-color: #f59e0b; 
            border-radius: 50%;
            width: 24px;
            height: 24px;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Celebration Balloons */
        .balloon-container {
            position: fixed;
            top: 100vh;
            width: 100%;
            height: 100vh;
            pointer-events: none;
            z-index: 100;
            display: none; /* Hidden by default */
        }
        .balloon {
            position: absolute;
            bottom: -100px;
            font-size: 3rem;
            animation: flyUp 5s ease-in forwards;
        }
        @keyframes flyUp {
            0% { bottom: -100px; transform: translateX(0) rotate(0); opacity: 1; }
            50% { transform: translateX(20px) rotate(10deg); }
            100% { bottom: 110vh; transform: translateX(-20px) rotate(-10deg); opacity: 0; }
        }
